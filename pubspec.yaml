name: gold_mine_trolls
description: Gold Mine Trolls - mobile casino-style game collection
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  logger: ^2.0.2+1
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.16
  audio_session: ^0.1.25
  just_audio: ^0.9.40
  appmetrica_plugin: ^3.4.0

dependency_overrides:
  shared_preferences_android: 2.4.12

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.6

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  fonts:
    - family: Gotham
      fonts:
        - asset: assets/fonts/Gotham-Black.otf
          weight: 900
    - family: Gilroy
      fonts:
        - asset: assets/fonts/Gilroy-ExtraBold.otf
          weight: 800

  assets:
    - assets/images/
    - assets/images/gold_vein/
    - assets/images/gold_vein/slots/
    - assets/images/miners_wheel_of_fortune/
    - assets/images/mine_depth_tower/
    - assets/images/mine_depth_tower/rooms/
    - assets/images/golden_avalanche/
    - assets/images/treasure_trail_ladder/
    - assets/images/chief_trolls_wheel/
    - assets/images/card_mine_21/
    - assets/images/card_mine_21/card/
    - assets/images/cautious_miner/
    - assets/images/onboarding/
    - assets/images/welcome_bonus/
    - assets/images/tutorial/
    - assets/images/main_screen/
    - assets/images/main_screen/game_banners/
    - assets/fonts/
    - assets/images/settings/
    - assets/images/paywall/
    - assets/images/shop/
    - assets/images/road_of_luck/
    - assets/images/info/
    - assets/images/common/
    - assets/sounds/
    - assets/sounds/common/
    - assets/sounds/card_mine_21/
    - assets/sounds/gold_vein/
    - assets/sounds/golden_avalanche/
    - assets/sounds/miners_wheel_of_fortune/
    - assets/sounds/chief_trolls_wheel/
    - assets/sounds/treasure_trail_ladder/
    - assets/sounds/cautious_miner/
    - assets/sounds/mine_depth_tower/
    - assets/sounds/road_of_luck/
    - assets/sounds/main_screen/
    - assets/sounds/shop/
    - assets/sounds/onboarding/
    - assets/sounds/welcome_bonus/
    - assets/sounds/settings/
    - assets/sounds/lose/
    - assets/sounds/winning/

flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
  windows:
    generate: true
  macos:
    generate: true
  linux:
    generate: true
  image_path: assets/images/icon.png
  remove_alpha_ios: true
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: assets/images/icon.png
  adaptive_icon_foreground_inset: 0

flutter_native_splash:
  background_image: assets/images/banner.png
  android: true
  ios: true
  web: true
  android_gravity: fill
  ios_content_mode: scaleAspectFill
  web_image_mode: cover
  fullscreen: true
  android_12:
    color: "#000000"
    image: assets/images/banner.png
    icon_background_color: "#000000"
